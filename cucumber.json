{
  "default": {
    "paths": ["tests/features/**/*.feature"],
    "import": ["tests/step-definitions/**/*.ts"],
    "format": ["progress"],
    "formatOptions": {
      "snippetInterface": "async-await"
    }
  }
}
